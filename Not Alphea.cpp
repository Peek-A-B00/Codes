#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <math.h>

#include <iostream>
using namespace std;

#define deb(a)     cout<<__LINE__<<"# "<<#a<<" -> "<<a<<endl;

typedef long long LL;
const int SIZE = 100;

int main()
{

    while(1==scanf("",&))
    {

        while(i<len && false == isalpha(s[i]))
        while(i<len && 0 == isalpha(s[i]))
        while(i<len && !isalpha(s[i]))

        printf("\n");
    }

    return 0;
}

